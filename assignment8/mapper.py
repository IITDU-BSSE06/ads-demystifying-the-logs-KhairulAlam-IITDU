#!/usr/bin/python

import sys

for line in sys.stdin:
	row = line.strip().split(" ")

	if len(row) == 10
		File = str(row[6])
		t1 = File.strip().split(".")
		ind = len(t1)-1
		t2 = str(t1[ind])
		print t2
